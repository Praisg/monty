#include "monty.h"

/**
 * nop - Does none at all.
 * @stack: Pointer to the toppart.
 * @line_numm: Line numb/ in the file where the opcode is located.
 */
void nop(stack_t **stack, unsigned int line_numm)
{
    (void)stack;
    (void)line_numm;
    /* do nothing */
}
